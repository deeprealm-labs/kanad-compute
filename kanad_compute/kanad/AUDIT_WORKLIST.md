# Kanad Rigorous Test-Audit Worklist (2026-06-16)

_855 pass, 0 failed, 0 errors, 16 skipped. ~38 untested defects (facade-only coverage). 17 HIGH bugs below; 23 medium/low bugs omitted for size._

**855 tests pass / 0 fail / 0 error** — but the suite has *facade-only coverage*; the adversarial module audit found the bugs below in untested paths.

## HIGH-severity bugs to fix

### H1. ConfigurationExplorer returns HF for every PES/barrier scan: compute_energy(solver=,backend=) swallowed by compute_energy(method='HF',**kwargs). H2 -1.1168 vs FCI -1.1373.
- **file:** `/home/mk/deeprealm/kanad/analysis/configuration_explorer.py:475`
- **fix:** Route through a real solver or builder energy_fn; drop the quantum-accurate claim.
- **severity:** high

### H2. DeterministicCI above-FCI on multireference, converged=True: layout vs to_matrix() but projection vs to_matrix(n_qubits,use_mo_basis), wrong sector. Stretched H2 -0.9316 vs FCI -0.9361, persists at subspace_dim=16.
- **file:** `/home/mk/deeprealm/kanad/solvers/deterministic_ci.py:377 vs :598`
- **fix:** Detect layout against the SAME matrix used for projection.
- **severity:** high

### H3. NEVPT2/CASSCF MRPT uses canonical MOs, ignores rotated mp2no/avas (mc.sort_mo on self.mf, active indexes active_space.mo_coeff). N2 mp2no(6) optimizes a different space.
- **file:** `/home/mk/deeprealm/kanad/builder/quantum_system.py:248`
- **fix:** Set mc.mo_coeff=active_space.mo_coeff, or raise for mp2no/avas.
- **severity:** high

### H4. SA-CASSCF excited-states uses sort_mo on canonical MOs, wrong for mp2no/avas. N2 mp2no(6) -107.6274 vs bare-CI -107.6372.
- **file:** `/home/mk/deeprealm/kanad/builder/quantum_system.py:671,686`
- **fix:** Build SA/CASCI on active_space.mo_coeff, or raise for mp2no/avas.
- **severity:** high

### H5. Bond.compute_energy VQE/SQD branches subscript a frozen SolverResult; only KRYLOV uses to_dict(). covalent H2 method=VQE raises TypeError. Same in ionic_bond.py:164,198.
- **file:** `/home/mk/deeprealm/kanad/core/bonds/covalent_bond.py:217,253`
- **fix:** Add to_dict() to VQE/SQD branches in both; params under extra['parameters'].
- **severity:** high

### H6. compute_quantum_dos(solver=sqd) discards the SQD spectrum: reads dead ['eigenvalues'] then single ['energy']. n_states=4 returns 1 eigenstate.
- **file:** `/home/mk/deeprealm/kanad/analysis/dos_calculator.py:568-571`
- **fix:** Read np.asarray(result_solver.get('energies')) before the single-energy fallback.
- **severity:** high

### H7. UVVisCalculator quantum_sqd/vibronic crash: to_dict() lists-ifies energies, excited minus ground is list-minus-float. H2 quantum_sqd raises TypeError.
- **file:** `/home/mk/deeprealm/kanad/solvers/excited_states_solver.py:552-557`
- **fix:** np.asarray(sqd_result['energies'],dtype=float) at 552 and spectroscopy.py:308.
- **severity:** high

### H8. Cloud backends ibm/bluequbit/ionq not BaseBackend (no subclass/name/estimate_expectation/sample); BaseSolver does self.backend.name -> AttributeError once token valid.
- **file:** `/home/mk/deeprealm/kanad/backends/factory.py; ibm/backend.py:77; bluequbit/backend.py:15; ionq/backend.py:15; solvers/base_solver.py:61`
- **fix:** Subclass BaseBackend + name + methods, or set backend_name from the requested string via getattr.
- **severity:** high

### H9. BondFactory.create_bond ignores distance for Atom+Atom (only str branch repositions). Reports 0.74 but physics at 5.0A.
- **file:** `/home/mk/deeprealm/kanad/core/bonds/bond_factory.py:83-88`
- **fix:** Reposition atom_2 along the bond axis at the requested distance, or raise on conflict.
- **severity:** high

### H10. Angstrom/Bohr mixup: legacy ReactionSimulator/QuantumReactionSimulator build gto.M(unit=bohr) from Angstrom. H-H@0.74A gives -0.8965 (0.39A) vs HF -1.1168.
- **file:** `/home/mk/deeprealm/kanad/reactions/reaction_dynamics.py:362 and reactions/quantum_reaction.py`
- **fix:** Convert atom.position Angstrom to Bohr once at init in both.
- **severity:** high

### H11. Quantum energy path falls back to a hardcoded Morse/LJ potential (D_e=0.17) on any exception, mixing force-field and ab-initio on one PES.
- **file:** `/home/mk/deeprealm/kanad/reactions/reaction_dynamics.py:336-338`
- **fix:** Do not substitute a different model; raise or NaN-mark and skip.
- **severity:** high

### H12. HybridSubspaceVQE (exported) broken+non-conformant: no BaseSolver, bare-dict solve(), reads .parameters from a SolverResult. Raises AttributeError on H2.
- **file:** `/home/mk/deeprealm/kanad/solvers/sampled_subspace_vqe.py:940`
- **fix:** Retire (remove from __all__) or convert to BaseSolver+SolverResult, read extra['parameters'].
- **severity:** high

### H13. IRC backward branch climbs uphill (descend=False) -> negative barriers (-0.037 Ha); fix never propagated; also breaks iter1 at TS leaving np.zeros.
- **file:** `/home/mk/deeprealm/kanad/reactions/quantum_reaction.py:560`
- **fix:** descend=True; seed first step displaced along the reaction coordinate.
- **severity:** high

### H14. diagonalize_pyscf spin_s branch precedes the FCI-tensor guard -> OOM/TypeError (full tensor; abs(_ss_of()=None - ss_t)).
- **file:** `/home/mk/deeprealm/kanad/core/ci/selected_ci.py:237 vs :289`
- **fix:** Budget-check before the spin_s branch; guard _ss_of None in the root loop.
- **severity:** high

### H15. ActiveHamiltonian breaks the protocol: docstring says duck-types as MolecularHamiltonian but neither subclasses it nor exposes .hamiltonian; _resolve_system raises TypeError.
- **file:** `/home/mk/deeprealm/kanad/core/active_space/integral_transform.py:41-43,137-147; solvers/base_solver.py:87-111`
- **fix:** Accept ActiveHamiltonian in _resolve_system (duck-type/.hamiltonian shim) or fix the docstring.
- **severity:** high

### H16. VQESolver ignores a passed ansatz= in bond mode (_init_ansatz runs unconditionally, default HEA). VQESolver(bond=H2,ansatz=LUCJ) ran HEA (12 not 5 params), N-leak.
- **file:** `/home/mk/deeprealm/kanad/solvers/vqe_solver.py:343-348`
- **fix:** Skip _init_ansatz() when an ansatz object is passed, or raise if both given.
- **severity:** high

### H17. LUCJAnsatz _supports_adjoint_gradient=True but circuit (raw PauliEvolutionGate+rzz, not decomposed) cannot be differentiated; gradient() raises; rzz generator None zeros Jastrow grads.
- **file:** `/home/mk/deeprealm/kanad/core/ansatze/lucj_ansatz.py:71,148,207`
- **fix:** Set the flag False or decompose() in build_circuit.
- **severity:** high

## Stale tests to rewrite

- **ConfigurationExplorer mock test (test_planck_analysis_audit_fixes.py)** — changed: Mock accepts solver/backend that real compute_energy(method='HF',**kwargs) discards; passes while production returns HF. → Drop the mock; real H2 Molecule, assert energy below HF.
- **UV-Vis via facade excited_states (CASCI route)** — changed: to_dict() lists-ifies energies; facade never hits the quantum path so the quantum_sqd crash and dos eigenvalues stale-key survive. → Add compute_excitations(method=quantum_sqd) on H2/LiH asserting finite positive excitation energies.
- **tests/unit/test_backend_protocol.py (statevector+planck only)** — changed: Cloud backends never subclassed BaseBackend; only local backends covered so the self.backend.name crash ships green. → Parametrize over all factory names; assert issubclass(BaseBackend), name, estimate_expectation/sample.
- **Examples subscripting solve(); Bond.compute_energy VQE/SQD** — changed: solve() returns a non-subscriptable SolverResult; VQE/SQD bond branches and examples still subscript and now raise. → Use result.energy/.to_dict()['energy']/.extra['parameters']; add covalent/ionic H2 VQE and SQD asserting energy below HF.
- **test_givens_rotation_ansatz.py test_sz_conservation** — changed: GivensRotationAnsatz is N- but NOT Sz-conserving (Givens(1,2)=pi/4 gives Sz=+0.5); label is wrong. → Relabel and add a random-theta test documenting nonzero Sz.

## Missing tests to add

- **Solver conformance + multireference**: solvers.__all__ isinstance(solve(),SolverResult) on H2; pin DeterministicCI/Lanczos/Smart/PhysicsVQE to FCI <1 uHa on stretched H2/H4; MRPT/SA-CASSCF on mp2no/avas match bare-CI on the same orbitals or raise; SQD select=bright not only ground state.
- **Reactions + ConfigurationExplorer**: Legacy reaction energy at correct Angstrom geometry; IRC both branches descend with barrier>=0; rate_constant no inf; ConfigurationExplorer vs real Molecule energy below HF + a PES point near FCI.
- **Gradients + core/ci**: Adjoint/param-shift flagged ansatze match finite-diff ~1e-7; spin_s large CAS no OOM/TypeError; out-of-sector det identical across engines; config_recovery no dupes valid_fraction<=1.0; S2 filtering; eigenvector-to-1RDM==FCI; over-62q arb-precision; unsorted-determinant build.
- **active_space + Hamiltonian + bonds + builder**: mp2no N2 conservation+bound+frontier fallback; avas closed/open-shell; ROHF OH frozen_core only doubly-occupied, SOMO-freeze raises; ValueError for over-filled/negative-frozen; bare ActiveHamiltonian works-or-raises; to_matrix vs JW agree on HF-determinant index; create_bond(Atom+Atom,0.74) real 0.74A; create_molecule(basis) honored-or-raises; solve() stable core keys + reselect VQE validates param length.

## Per-module audit findings (incl. medium/low)

### kanad.solvers (14 exported solvers, BaseSolver unified protocol)
_protocol: 13 of 14 exported solver classes conform structurally: they inherit BaseSolver, build self.backend via kanad.backends.factory.make_backend (setting self.backend_name = self.backend.name), and their public solve() returns a SolverResult (verified by inspection + live H2 runs for VQESolver, CISolver, DeterministicCI, LanczosSolver, SmartSolver, PhysicsVQE, SampledSubspaceVQE, qEOMVQE, ExcitedStatesSolver, VarQITESolver; HardwareVQE returns SolverResult by inspection). The lone exception is HybridSubspaceVQE, which does NOT inherit BaseSolver, never builds self.backend, and returns a bare dict fr_

- [high/silent_wrong_result] DeterministicCI (alias SQDSolver) silently returns a wrong, above-FCI energy on multireference systems, with converged=True — `/home/mk/deeprealm/kanad/solvers/deterministic_ci.py` — On stretched H2 (d=2.5 A, STO-3G, 4 qubits -- strongly multireference) DeterministicCI returns E=-0.931639 Ha vs FCI=-0.936055 Ha (+4.42 mHa) and reports converged=True. The error PERSISTS even at subspace_dim=16 (the full 16-dim Hilbert space), so it is not a truncation-budget issue -- the generated determinant set is in the WRONG spin sector. _generate_subspace_basis produced determinant indices
- [high/bug] HybridSubspaceVQE is a public exported solver that is broken (crashes for every input) and violates the unified protocol — `/home/mk/deeprealm/kanad/solvers/sampled_subspace_vqe.py` — HybridSubspaceVQE (class at line 743) is exported in solvers/__init__.py __all__ but (a) does NOT inherit BaseSolver, (b) never builds self.backend / make_backend, and (c) solve() returns a bare dict (line 1000), not a SolverResult. Worse, it is currently broken at runtime: solve() (line 925) calls self.hardware_vqe.solve_local(...) which now returns a SolverResult, then reads vqe_result.parameter
- [medium/silent_wrong_result] HybridSubspaceVQE truncates the determinant subspace by nondeterministic set ordering, risking dropped dominant configurations — `/home/mk/deeprealm/kanad/solvers/sampled_subspace_vqe.py` — In HybridSubspaceVQE.solve() line 955-956: all_configs = list(set(det_configs + sampled_configs)); valid_configs = all_configs[:self.max_configs]. Configurations are deduped via a Python set then truncated to max_configs by the set's iteration order, which is hash-seed dependent and unrelated to determinant importance/weight. For a system whose dominant determinant is not within the first max_conf
- [medium/protocol_violation] Entire VQE-family energy path bypasses the BaseBackend.estimate_expectation protocol it is documented to use — `/home/mk/deeprealm/kanad/solvers/vqe_solver.py` — base_backend.py states solvers consume backends through estimate_expectation/sample. VQESolver._compute_energy_statevector (line 1097) and _compute_energy_quantum (line 1350) never call self.backend.estimate_expectation; they build qiskit Statevector directly, branch on self.backend_name=='planck', and dispatch cloud execution through legacy self._ibm_backend/_bluequbit_backend/_ionq_backend attri
- [medium/silent_wrong_result] SmartSolver classical-FCI path hardcodes RHF, silently wrong for open-shell / radical molecules — `/home/mk/deeprealm/kanad/solvers/smart_solver.py` — _solve_classical_fci (line 204-232) does mf = scf.RHF(mol) unconditionally before fci.FCI(mf), ignoring molecule spin/multiplicity. For an open-shell system (e.g. a doublet radical, O2 triplet) RHF is the wrong reference and the returned 'exact (FCI)' energy / HF reference are silently wrong. By contrast PhysicsVQE (physics_vqe.py line 277-285) correctly picks ROHF/UHF for mol.spin != 0. SmartSolv
- [medium/silent_wrong_result] DeterministicCI auto-reduces subspace_dim to 4 for >8-qubit systems without surfacing it in the convergence/validation status — `/home/mk/deeprealm/kanad/solvers/deterministic_ci.py` — solve() line 1112-1115: for hilbert_dim>256 (>8 qubits) it silently sets self.subspace_dim = min(4, subspace_dim). A 4-determinant subspace on a >8-qubit system captures almost no correlation, yet results['converged'] stays True (line 1157, 'SQD always converges') and the only validation gate is energy<=HF. Combined with the multireference layout bug above, a >8-qubit multireference molecule can r
- [low/bug] DeterministicCI IBM-hardware projection path crashes (does not fall back) on any real molecular Hamiltonian — `/home/mk/deeprealm/kanad/solvers/deterministic_ci.py` — _run_ibm_measurements (line 850) samples circuits in the Z basis only (meas.get_counts(), no basis rotation) and feeds them to _calculate_expectation_from_counts -> core expectation_from_counts, which (correctly, as an honesty guard) raises NotImplementedError on any X/Y Pauli term. Every JW-mapped molecular Hamiltonian has X/Y terms, so the IBM SQD path raises rather than producing a diagonal-onl
- [low/risk] Debug print() statements left in the VQE statevector hot path — `/home/mk/deeprealm/kanad/solvers/vqe_solver.py` — _compute_energy_statevector emits unconditional print()s on first call: lines 1149-1150 ('Statevector: ... components', 'Bound circuit: ...'), lines 1183/1194/1201-1202 ('Using provided SparsePauliOp', identity coefficient, nuclear repulsion), and line 1247 first three energy evals. These fire on every VQESolver/CISolver run (confirmed in the H2 smoke test output) regardless of any verbosity flag,
- [medium/missing_test] No smoke/conformance test asserts every exported solver returns a SolverResult — `/home/mk/deeprealm/kanad/solvers/__init__.py` — HybridSubspaceVQE shipped as a public exported solver that crashes on every input and returns a bare dict, undetected. There is no parametrized test that, for each name in solvers.__all__ that is a solver class, instantiates it on a tiny system (H2) and asserts isinstance(result, SolverResult). Such a test would have caught both the HybridSubspaceVQE protocol violation and its AttributeError regre

### kanad/analysis (analyzers consuming the unified SolverResult)
_protocol: PARTIAL. The unified protocol (core/solver_result.py) makes solve() return a frozen SolverResult that is NOT subscriptable, has no .get/.__contains__, and whose .to_dict() recursively JSON-ifies numpy arrays into Python lists. Most analyzers were updated correctly: thermochemistry, spectroscopy (lines 302/921), raman, and dos_calculator all call .to_dict() or guard with hasattr(...,'to_dict'); property_calculator discards the result and reads the 1-RDM from the Hamiltonian; configuration_explorer._get_molecular_energy unwraps .energy / dict / scalar (tested). HOWEVER two real defects survive a_

- [high/silent_wrong_result] compute_quantum_dos(solver='sqd') silently discards the SQD subspace spectrum and returns a single-state DOS — `/home/mk/deeprealm/kanad/analysis/dos_calculator.py` — Lines 568-571: `if 'eigenvalues' in result_solver: ... elif 'energy' in result_solver: eigenvalues = np.array([result_solver['energy']])`. DeterministicCI's SolverResult.to_dict() exposes the multi-state spectrum under 'energies' (and 'states'/'excited_state_energies'), NEVER 'eigenvalues', so the first branch is dead and the code falls through to the single scalar 'energy'. REPRODUCED: compute_qu
- [high/bug] UVVisCalculator quantum_sqd and quantum vibronic spectrum crash because to_dict() turns the energies ndarray into a list and the solver then does list-minus-float arithmetic — `/home/mk/deeprealm/kanad/solvers/excited_states_solver.py` — spectroscopy.py:302 (_compute_quantum_sqd) and :921 (compute_quantum_vibronic_spectrum) call ExcitedStatesSolver(method='sqd').solve().to_dict(). Inside ExcitedStatesSolver._solve_sqd (lines 549-557) the upstream DeterministicCI result is also flattened with .to_dict(), which runs _jsonable() and converts 'energies' from ndarray to a Python list. Lines 552-557: `energies = sqd_result['energies']; 
- [high/bug] Bond.compute_energy VQE/SQD branches subscript a SolverResult directly; only the KRYLOV branch was fixed — `/home/mk/deeprealm/kanad/core/bonds/covalent_bond.py` — covalent_bond.py:214-227 (VQE) does `vqe_result = vqe.solve()` then `vqe_result['energy']`, `['converged']`, `['iterations']`, `['parameters']`, `['energy_history']`; lines 250-261 (SQD) do `sqd_result = solver.solve()` then `sqd_result['energy']` / `.get(...)`. But line 272 (KRYLOV) correctly does `solver.solve().to_dict()`. SolverResult is a frozen dataclass: not subscriptable, no .get(). REPROD
- [medium/missing_test] No test coverage for any quantum-solver-consuming analyzer path (SQD UV-Vis / vibronic / quantum DOS) — `/home/mk/deeprealm/kanad/tests/validation/test_planck_analysis_audit_fixes.py` — grep across tests/ and benchmarks/ shows NO test exercises UVVisCalculator.compute_excitations(method='quantum_sqd'), compute_quantum_vibronic_spectrum, or compute_quantum_dos(solver='sqd'). benchmarks/planck_full_analysis_audit.py validated UV-Vis only via the builder facade sysobj.excited_states(n_states=4) (a CASCI/PySCF route) and NMR/Raman only via method='HF' -- it never drove the analyzers 
- [low/protocol_violation] EnergyAnalyzer.analyze_electron_correlation still subscripts its input dict; breaks if handed the new SolverResult — `/home/mk/deeprealm/kanad/analysis/energy_analysis.py` — Lines 601-616: analyze_electron_correlation(self, vqe_result: Dict, hf_energy) does `vqe_energy = vqe_result['energy']`. Signature/docstring still say 'VQE result dictionary', but VQESolver.solve() now returns a non-subscriptable SolverResult. `EnergyAnalyzer(...).analyze_electron_correlation(vqe.solve(), hf)` raises TypeError: 'SolverResult' object is not subscriptable. No internal callers (grep 
- [low/risk] uncertainty.py docstring example and compare_runs API assume the old dict result shape — `/home/mk/deeprealm/kanad/analysis/uncertainty.py` — Line 185 docstring shows `Example: lambda n: vqe.solve(n_shots=n)['energy']`, which now raises TypeError (SolverResult not subscriptable) if copied. compare_runs(energy_results: List[Dict[str, float]]) at line 241 does r['energy'] (line 261), so passing a list of SolverResults breaks. UncertaintyAnalyzer never calls .solve() itself, so this is a documentation/contract risk, not an internal crash. 

### kanad/builder (MolecularBuilder, QuantumSystem, SystemSpec) + solvers/solver_router.py
_protocol: Conforms to the new-protocol wiring at the consumption boundary: every inner solver call that returns a SolverResult (VQESolver.solve, SamplingSQDSolver.solve / solve_iterative) is correctly flattened via .to_dict() before the builder mutates it (quantum_system.py:322, 361, 367); solve_excited_states_iterative correctly returns a plain dict and is consumed as such (no .to_dict()). QuantumSystem is intentionally NOT a BaseSolver subclass — it is a facade that produces dicts, so the abstract solve() contract does not apply. Route resolution (auto->ci/vqe/sqd via SolverRouter on 2*n_active_orbita_

- [high/silent_wrong_result] NEVPT2/CASSCF MRPT path uses canonical HF orbitals (sort_mo on self.mf), silently ignoring the rotated active-space basis for mp2no/avas — `/home/mk/deeprealm/kanad/builder/quantum_system.py` — _solve_ci_mrpt (line 248): `mo = mc.sort_mo([i + 1 for i in active])` builds CASSCF/CASCI on self.mf's CANONICAL MOs, but `active = active_space.active_indices` are indices into active_space.mo_coeff, which for mp2no and avas is a ROTATED basis (C_no / AVAS orbitals), NOT the canonical MOs. The comment 'Pin the SAME active orbitals Kanad selected' is false for rotated spaces. Verified on N2/sto-3g
- [high/silent_wrong_result] SA-CASSCF / CASCI-nroots excited-states path also uses sort_mo on canonical MOs, wrong basis for mp2no/avas — `/home/mk/deeprealm/kanad/builder/quantum_system.py` — _excited_states_sa_casscf (lines 671 and 686): `sa.sort_mo([i + 1 for i in active])` and `states_mc.sort_mo([i + 1 for i in active])` both operate on self.mf canonical MOs. Verified on N2/sto-3g mp2no(6): `.active_space('mp2no').excited_states(n_states=2, orbital_optimization=True)` returns ground E=-107.62744 while bare CI on the mp2no Hamiltonian gives -107.63724 (~10 mHa off, plus the energies/
- [medium/silent_wrong_result] excited_states(select='bright'/'brightest') silently drops all excited states on the SQD route (no oscillator strengths) — `/home/mk/deeprealm/kanad/builder/quantum_system.py` — _annotate_and_select (lines 591-621): on the SQD route there are no oscillator_strengths or state_extent_r2 (the SQD spectrum only returns energies). `_f(i)` defaults missing osc to 0.0, so select='bright' keeps only the ground state (verified: H2 SQD excited_states(n_states=3, select='bright') returns n_states=1, selected_indices=[0]). select='brightest' arbitrarily keeps index 1 (all f=0), and s
- [low/bug] excited_states(spin=<impossible manifold>) crashes with bare IndexError instead of a clear error — `/home/mk/deeprealm/kanad/builder/quantum_system.py` — _excited_states_ci line 470 `e0 = energies[0]`. When fci_excited_states finds zero roots of the requested multiplicity (e.g. excited_states(n_states=2, spin=2.0) on a 2-electron system where S=2 is impossible), it returns an empty list (after a warning), and line 470 raises 'IndexError: list index out of range'. Verified. Should validate len(energies)>0 and raise a descriptive ValueError. Needs a 
- [medium/risk] VQE warm-start passes initial_parameters with no length validation against the current ansatz — `/home/mk/deeprealm/kanad/builder/quantum_system.py` — _solve_vqe (lines 318-319) forwards warm_state as initial_parameters whenever np.ndim(warm_state)==1, with no check that len == solver.n_parameters. VQESolver._solve_standard_vqe (vqe_solver.py:2009) accepts user initial_parameters verbatim (no length guard). Under the default freeze policy the parameter count is constant so this is safe, but under active_space policy='reselect' along a scan/MD (e
- [low/bug] SQD quantum_only mode silently ignores warm_state (no warm-start through energy_fn scans) — `/home/mk/deeprealm/kanad/builder/quantum_system.py` — _solve_sqd (lines 358-364): the quantum_only=True branch calls solver.solve(ansatz_circuit=circuit) which has no seed_determinants parameter, so the incoming warm_state is dropped entirely. The non-quantum_only branch threads warm_state via solve_iterative(seed_determinants=...). Along an energy_fn/MD scan in quantum_only mode the determinant subspace is never warm-started; not wrong, but the docu
- [medium/risk] _build_sampling_circuit circuit target_sz can diverge from the SQD solver's filter target_sz — `/home/mk/deeprealm/kanad/builder/quantum_system.py` — _make_sqd_solver sets the (N, Sz) filter target_sz = self.spec.spin/2.0 (line 339), while _build_sampling_circuit sets the CIRCUIT target_sz = ansatz_kwargs.get('target_sz', self.spec.spin/2.0) (line 834). If a user sets .ansatz(target_sz=X) different from spin/2, the LUCJ circuit prepares an Sz=X reference but the solver filters for Sz=spin/2, which can drop all samples ('No samples survived (N,S
- [medium/protocol_violation] Result dict shape is route-dependent (converged/iterations/hf_energy missing on ci and vqe routes) — `/home/mk/deeprealm/kanad/builder/quantum_system.py` — solve() returns {energy, method, fci_vector, solver, n_qubits} for ci; {energy, parameters, method, raw, solver, n_qubits} for vqe; and a fully-flattened SolverResult (converged, hf_energy, correlation_energy, analysis, states, determinants, energy_history, ...) for sqd. The unified-protocol intent is a stable core (energy/converged/iterations/hf_energy). Downstream consumers that read res['conver
- [low/risk] Builder VQE route does not disable inner-solver analysis/optimization; triggers attribute-error warnings and wasted work — `/home/mk/deeprealm/kanad/builder/quantum_system.py` — _solve_vqe (line 307) constructs VQESolver without enable_analysis=False/enable_optimization=False, so the inner VQE runs BaseSolver analysis hooks, emitting 'VQESolver object has no attribute energy_analyzer/bonding_analyzer/property_calculator' warnings and 'Error storing enhanced data: SimpleNamespace has no attribute n_atoms' on every builder VQE solve (observed in test run). Energy is unaffec
- [low/silent_wrong_result] Full-space observables('all') computes polarizability via a fresh VQE on a CI/SQD-route system (provenance mismatch) — `/home/mk/deeprealm/kanad/builder/quantum_system.py` — observables() line 1014: for a non-SQD route it calls pc.compute_polarizability(wavefunction='vqe'), which (verified) launches a fresh VQE optimization rather than using the CI/NEVPT2 density that produced the reported energy. The polarizability is then VQE-derived while the energy/dipole are CI-derived — inconsistent provenance, returned without a route-mismatch flag. The docstring flags which='a
- [high/missing_test] No tests for the multireference paths (pt2_correction / orbital_optimization / SA-CASSCF) or for excited_states select= filtering — `/home/mk/deeprealm/kanad/tests/validation/test_molecular_builder.py` — grep across tests/ shows ZERO references to pt2_correction, orbital_optimization, nevpt2, sa-casscf, or select=. The two confirmed silent-wrong-result bugs above (rotated-basis MRPT/SA-CASSCF, and SQD select='bright' dropping all states) live entirely in untested code. Add: (1) a test asserting _solve_ci_mrpt/_excited_states_sa_casscf on an mp2no/avas active space either matches the bare-CI energy
- [low/risk] SystemSpec strategy comment and _resolve_active_space error message omit 'avas' (handled but undocumented in those spots) — `/home/mk/deeprealm/kanad/builder/system_spec.py` — active_space_strategy field comment (line 42) lists 'auto|full|frozen_core|frontier|mp2no|manual' and the ValueError at lines 164-167 lists the same set, both omitting 'avas', even though avas IS a valid handled strategy (line 148-154) and the MolecularBuilder.active_space docstring advertises it. A user passing a typo near avas gets a misleading 'expected one of ...' message that doesn't mention 

### core/ci (selected_ci.py, slater_condon.py, config_recovery.py) — consumed by solvers/sampling_sqd.py (SamplingSQDSolver, a BaseSolver)
_protocol: core/ci/ contains free functions (diagonalizers, Slater-Condon engine, config recovery), not BaseSolver subclasses, so they do not themselves implement solve()/the unified protocol — their consumer SamplingSQDSolver does. The dict contract they return (energy/eigenvector/determinants/n_determinants, plus s_squared/spin_enforced on the pyscf path) is honored by both diagonalize_pyscf and diagonalize_custom, EXCEPT diagonalize_custom omits s_squared/spin_enforced (the pyscf path's extra keys) and does not drop out-of-(N,Sz)-sector determinants, so the two "interchangeable" engines return differe_

- [high/bug] diagonalize_pyscf: spin_s branch precedes the large-CAS FCI-tensor guard and crashes/OOMs on large active spaces — `/home/mk/deeprealm/kanad/core/ci/selected_ci.py` — The `if spin_s is not None:` block (line 237) runs BEFORE the `_FCI_TENSOR_MAX` guard (line 289). When spin_s is set AND n_fci_a*n_fci_b exceeds the budget (high-q / large dilute CAS — exactly the regime the guard exists for), the spin_s branch (a) materializes the full FCI tensor via np.zeros((n_fci_a,n_fci_b)) + direct_spin1.contract_2e (lines 244-248 / 253-254), the very OOM the high-q guard wa
- [medium/silent_wrong_result] diagonalize_custom does not drop out-of-(N,Sz)-sector determinants, desyncing determinants/eigenvector vs diagonalize_pyscf — `/home/mk/deeprealm/kanad/core/ci/selected_ci.py` — diagonalize_pyscf explicitly drops determinants outside the (n_a,n_b) sector and rebinds the returned list 1:1 with the eigenvector (CORE_BUGS B16, lines 174-184). diagonalize_custom has no such guard: I passed [0b0011, 0b1100, 0b0111] (last is a 3-electron det) and it returned n_determinants=3 with the spurious det retained in both `determinants` and `eigenvector` (coeff ~0 here because it's disc
- [medium/silent_wrong_result] Configuration recovery returns duplicate determinants, inflating valid_fraction / n_recovered statistics (can exceed 1.0) — `/home/mk/deeprealm/kanad/core/ci/config_recovery.py` — _filter_with_recovery appends each recovered bitstring without dedup, and _filter_with_iterative_recovery does current_valid.extend(added) without deduping the returned list (only current_dets, used for diagonalization, is deduped via sorted(set(...))). When multiple noisy samples recover to the same valid determinant, the returned array contains duplicates: I fed 3 distinct wrong-N samples that a
- [medium/protocol_violation] SamplingSQDSolver does not set _is_correlated, so the protocol's 'energy ≤ HF' validation gate silently never runs for SQD — `/home/mk/deeprealm/kanad/solvers/sampling_sqd.py` — BaseSolver.validate_results() (base_solver.py line 309) only performs the energy-below-HF sanity check when `hasattr(self,'_is_correlated') and self._is_correlated`. lanczos_solver, deterministic_ci, and vqe_solver all set self._is_correlated=True; SamplingSQDSolver does not (confirmed '_is_correlated' not in dir(SamplingSQDSolver)). SQD is a correlated method whose energy should lie at or below H
- [low/risk] diagonalize_pyscf high-q sparse branch bypasses _finalize, so the spin-contamination warning never fires there — `/home/mk/deeprealm/kanad/core/ci/selected_ci.py` — The high-q sparse branch (lines 289-310) builds its own result dict with s_squared=None, spin_enforced=False and returns directly, skipping _finalize() and thus the spin-contamination check in _finalize (lines 213-222). At that scale _ss_of would return None anyway (full tensor too large), so the warning is genuinely uncomputable — but the energy can fall below the spin-pure reference if a higher-
- [low/risk] _recover_spin_sector silently returns a determinant with fewer than n_target bits when n_target > n_orbitals — `/home/mk/deeprealm/kanad/core/ci/config_recovery.py` — With delta<0 and n_set=-delta exceeding len(unoccupied), the code sets unoccupied[:n_set] (all available) and returns a det with bit-count < n_target without signalling failure: _recover_spin_sector(0,4,6,...) returns 0b1111 (4 bits, requested 6). Not reachable in normal flow (n_target = n_alpha/n_beta ≤ n_orbitals always for a valid active space), so low severity — defensive-programming gap only.
- [low/missing_test] Several validated-correct but untested code paths in core/ci — `/home/mk/deeprealm/kanad/tests/validation/test_tier1_fixes.py` — Existing coverage: C1 custom-vs-pyscf αβ-doubles (test_tier1_fixes) and _recover_spin_sector basics (test_lucj_and_recovery). UNCOVERED by tests (I validated these manually this session, all correct): fci_excited_states ⟨S²⟩ singlet/triplet filtering; s_squared_of_subspace; diagonalize_pyscf spin_s singlet/triplet selection; the interleave→block sign vs brute-force parity; the sign-corrected eigen

### kanad.core.ansatze (LUCJ, LUCJFfsim, GivensRotation, GivensSD, HEA, PhysicsDriven, CustomCircuit) vs solvers/base_solver.py + VQESolver consumption
_protocol: Ansatze are not BaseSolver subclasses; they are inputs consumed by solvers. The de-facto ansatz protocol the solver depends on is: build_circuit(initial_state=None), one of {n_parameters, num_parameters, get_num_parameters}, a .parameters list, .circuit, and gradient-routing flags _supports_parameter_shift / _supports_adjoint_gradient. Core chemistry path is sound: GivensSD reaches H2 FCI (err ~1.7e-4 mHa) and its adjoint gradient matches finite-diff to 4e-10; HEA reaches H2 FCI; N-conservation holds for GivensRotation/GivensSD/LUCJ (closed and open shell) at machine precision; LUCJ Jastrow ZZ_

- [high/bug] LUCJAnsatz declares _supports_adjoint_gradient=True but its built circuit (raw PauliEvolutionGate + rzz, never decomposed) cannot be differentiated by AdjointGradientCalculator — it crashes with ValueError, and even without the crash would silently drop all Jastrow/orbital gradients. — `/home/mk/deeprealm/kanad/core/ansatze/lucj_ansatz.py` — build_circuit() leaves PauliEvolutionGate (orbital rotations) and rzz/rz (Jastrow) in the circuit; unlike GivensSDAnsatz it does NOT call qc.decompose(). With _supports_adjoint_gradient=True, VQESolver(hamiltonian=ham, ansatz=LUCJAnsatz(...), optimizer='L-BFGS-B') routes to the adjoint path. Reproduced: AdjointGradientCalculator.gradient() raises 'ValueError: Unsupported operator type for evolutio
- [high/silent_wrong_result] VQESolver silently ignores a directly-passed ansatz= object when bond= is used. — `/home/mk/deeprealm/kanad/solvers/vqe_solver.py` — When _api_mode=='bond' (constructor lines 343-348), __init__ calls _init_ansatz() unconditionally, which builds the ansatz from ansatz_type (default 'hardware_efficient') and never reads the ansatz= argument. ansatz= is only honored in components mode (hamiltonian= + ansatz=). Reproduced: VQESolver(bond=H2, ansatz=LUCJAnsatz(4,2)) ran HEA (solver.n_parameters=12, not LUCJ's 5) and returned a parti
- [medium/silent_wrong_result] GivensRotationAnsatz is NOT spin-conserving (S_z leaks at nonzero theta), contradicting its source docstring and the test class title. — `/home/mk/deeprealm/kanad/core/ansatze/givens_rotation_ansatz.py` — Brick-wall pairs include qubit pairs that straddle the spin boundary in the interleaved-JW convention (alpha at 2p, beta at 2p+1) used everywhere else in kanad — e.g. pair (1,2) couples beta-orbital-0 (q1) to alpha-orbital-1 (q2), a spin-flip excitation. Reproduced minimal case: 4q/2e, single Givens(1,2)=pi/4 on HF |0011> -> superposition with <Sz>=+0.5 (HF Sz=0). Random-theta on 8q/4e gives <Sz>=
- [medium/silent_wrong_result] PhysicsDrivenAnsatz selects excitations using block (non-interleaved) spin-orbital indices while the JW Hamiltonian uses interleaved indices, producing S_z-non-conserving excitations on systems with >2 spatial orbitals. — `/home/mk/deeprealm/kanad/core/ansatze/physics_driven_ansatz.py` — _select_important_excitations (lines 90-108) builds occupied=range(0,n_occ), virtual=range(n_occ,n_qubits) as if spin-orbitals are a contiguous block, then passes those raw indices to build_excitation_generator, which interprets them as JW (interleaved) qubit indices. Reproduced: 6q/2e gives excitations like (0,1)->(2,4) whose JW image leaks Sz (<Sz>=0.11 at random theta) instead of 0. Mitigated i
- [medium/risk] Parameter binding maps the optimizer vector positionally to Qiskit's lexicographically-sorted circuit.parameters, but several ansatze build parameter names whose insertion order != sorted order (HEA, LUCJ, CustomCircuit). — `/home/mk/deeprealm/kanad/core/ansatze/hardware_efficient_ansatz.py` — Solver binds via {qiskit_circuit.parameters[i]: parameters[i]} (vqe_solver.py:1127, :874) where circuit.parameters is sorted by name. HEA names like 'theta_0_ry_2' vs 'theta_0_ry_10' sort lexicographically so insertion order (ansatz.parameters) diverges from bind order at index 2; LUCJ 'K_jas_*' sorts before 'theta_orb_*'. GivensRotation/GivensSD avoid this by using ParameterVector (verified order
- [low/protocol_violation] LUCJAnsatz exposes the param-count via num_parameters (property) but lacks n_parameters and get_num_parameters that every other ansatz and the solver probe prefer. — `/home/mk/deeprealm/kanad/core/ansatze/lucj_ansatz.py` — Contract probe matrix (verified): HEA/GivensRotation/GivensSD/Custom expose n_parameters + get_num_parameters; PhysicsDriven exposes n_parameters; LUCJ exposes ONLY num_parameters. The solver's components-mode probe (vqe_solver.py:463-468) checks n_parameters -> circuit.get_num_parameters -> num_parameters, so LUCJ resolves correctly to 5, but the inconsistency is a footgun for any consumer using 
- [medium/missing_test] No test asserts adjoint/parameter-shift gradient correctness for ansatze that set the gradient-support flags; the false LUCJ flag (and any future regression) goes uncaught. — `/home/mk/deeprealm/kanad/tests/unit/test_givens_rotation_ansatz.py` — The suite checks N-conservation, theta=0=HF, and the boolean value of _supports_parameter_shift, but never verifies that an ansatz declaring _supports_adjoint_gradient=True actually yields a finite-diff-matching gradient through AdjointGradientCalculator, nor that _supports_parameter_shift=True ansatze match finite-diff via the parameter-shift rule. A single parametrized test (for every ansatz: bu
- [low/risk] LUCJAnsatz computes self.n_occ_spatial but never uses it; HF init uses n_alpha/n_beta. Dead code that encodes a closed-shell-only assumption and could mask intent for open-shell. — `/home/mk/deeprealm/kanad/core/ansatze/lucj_ansatz.py` — Line 117 sets self.n_occ_spatial = n_electrons // 2 (closed-shell assumption) but it is never referenced anywhere (grep confirms no internal/external use). For open-shell (target_sz != 0) this value is physically wrong (ignores n_alpha != n_beta); it being dead is the only reason it does no harm. Recommend removing it to avoid a future maintainer wiring an incorrect value into the orbital loops. N
- [low/risk] PhysicsDrivenAnsatz.get_initial_parameters indexes get_orbital_energies() with spin-orbital indices that can exceed the spatial-orbital array length; guarded only because no Hamiltonian exposes get_orbital_energies. — `/home/mk/deeprealm/kanad/core/ansatze/physics_driven_ansatz.py` — Lines 217-218 compute orb_energies[i]+orb_energies[j]-orb_energies[a]-orb_energies[b] with i,j,a,b being block spin-orbital indices up to n_qubits-1, but get_orbital_energies (core/scf.py) returns spatial orbital energies (length n_spatial). hasattr(self.hamiltonian,'get_orbital_energies') is False for all Hamiltonians (it lives on SCF, not Hamiltonian), so the code always falls back to np.zeros a

### kanad/core/hamiltonians, kanad/core/integrals, kanad/core/active_space (audited against solvers/base_solver.py unified protocol)
_protocol: MOSTLY CONFORMANT with one documented-contract violation. The unified protocol (BaseSolver._resolve_system) accepts: Molecule, the concrete/abstract MolecularHamiltonian, or any object exposing `.hamiltonian`. CovalentHamiltonian/IonicHamiltonian/MetallicHamiltonian subclass the abstract MolecularHamiltonian and are accepted; their `to_sparse_hamiltonian(mapper)`, `n_electrons`, `n_orbitals`, `solve_scf`, `mf` surfaces line up with what solvers + get_reference_energy expect. CRITICAL EXCEPTION: ActiveHamiltonian is explicitly documented (core/active_space/integral_transform.py:41-43, 137-147) _

- [high/protocol_violation] ActiveHamiltonian breaks the unified solver protocol when passed directly to a solver — `/home/mk/deeprealm/kanad/core/active_space/integral_transform.py` — ActiveHamiltonian's docstring (lines 41-43 and 137-147) promises it 'duck-types as MolecularHamiltonian' so 'downstream solvers (VQESolver, PhysicsVQE, SQDSolver) consume it without modification.' But it neither subclasses MolecularHamiltonian (abstract base at core/hamiltonians/molecular_hamiltonian.py, or the concrete kanad.core.molecule.MolecularHamiltonian) nor exposes a `.hamiltonian` attribu
- [medium/risk] Dead, convention-inconsistent O(n^4) manual Pauli-construction block in PauliConverter.to_sparse_pauli_op — `/home/mk/deeprealm/kanad/core/hamiltonians/pauli_converter.py` — to_sparse_pauli_op builds a full `pauli_dict` across lines 64-158 (including a 4-deep n_orbitals loop for the two-body terms, lines 114-149) and then NEVER returns it — the function always returns a freshly rebuilt operator via build_molecular_hamiltonian_bk/jw at lines 170-176 (after the qiskit-nature early-return at line 56). The only `return pauli_dict` (line 210) is in the separate to_pauli_di
- [medium/silent_wrong_result] CovalentHamiltonian.to_matrix and to_sparse_hamiltonian use different spin-orbital orderings (block vs interleaved) — `/home/mk/deeprealm/kanad/core/hamiltonians/covalent_hamiltonian.py` — to_matrix() builds the Fock-space Hamiltonian in BLOCKED spin ordering: qubits [0..n_orb-1]=alpha, [n_orb..2n_orb-1]=beta (documented at lines 477-481; built at lines 514-544 via n_orb+i offsets). to_sparse_hamiltonian() -> build_molecular_hamiltonian_pauli -> build_molecular_hamiltonian_jw uses INTERLEAVED ordering 2p (alpha), 2p+1 (beta) (jordan_wigner.py:282-321). I verified the two paths give 
- [medium/missing_test] mp2_natural_orbitals selector has zero regression test coverage (electron-conservation, max_orbitals spill, near-single-reference frontier fallback) — `/home/mk/deeprealm/kanad/core/active_space/selector.py` — tests/validation/test_active_space.py covers manual/frozen_core/frontier and the LiH chemical-accuracy invariant, but NOTHING exercises mp2_natural_orbitals. Three non-trivial, recently-touched branches are untested: (a) electron conservation when partially-occupied NOs are kept active (lines 285-316) — the CORE_BUGS B5 fix; (b) the spill-to-frozen/virtual demotion logic (lines 297-308); (c) the n
- [medium/missing_test] avas selector (incl. the B3 5-tuple-unpacking fix and open-shell SOMO assertion) has zero test coverage — `/home/mk/deeprealm/kanad/core/active_space/selector.py` — avas() (lines 347-408) is documented as having previously crashed on every call due to a ValueError from unpacking pyscf's module-level avas() (returns 5 values) into 3 names — the fix catches trailing weights with `*_` (lines 381-383). It also has an open-shell guard (lines 392-399) asserting the inactive electron count is even. None of this is tested. I verified avas(['O 2p']) on H2O works end-t
- [low/missing_test] Open-shell active-space guards (SOMO-freeze rejection, doubly-occupied frozen-core check) are untested — `/home/mk/deeprealm/kanad/core/active_space/selector.py` — The open-shell frozen-core guard in manual() (lines 146-155, requiring every frozen orbital to have mo_occ ~= 2) and the _is_open_shell detection (lines 105-107) are load-bearing for correctness of the 2J-K frozen-core transform on ROHF references, but no test covers them. I verified on the OH radical (ROHF, 9 e): frozen_core() correctly freezes only the doubly-occupied O 1s, and manual(frozen=[0,
- [low/missing_test] ActiveSpace/CovalentHamiltonian over-fill and negative-electron guards are untested — `/home/mk/deeprealm/kanad/core/active_space/selector.py` — ActiveSpace.__post_init__ (lines 62-74) and manual() (lines 159-164) raise on negative or over-filled active-electron counts; CovalentHamiltonian.__init__ (lines 182-193, CORE_BUGS B7) raises the same for the Hi-VQE active_orbitals path. These prevent a silent unphysical electron count (occupied orbitals dropped to virtual). I verified manual(frozen=[], active=[3,4,5,6]) on H2O correctly raises 'a
- [low/risk] get_reference_energy / validate_results HF-reference assumption may not be an upper bound for rotated-basis active spaces (mp2no/avas) — `/home/mk/deeprealm/kanad/solvers/base_solver.py` — get_reference_energy (lines 278-280) returns mf.e_tot for an ActiveHamiltonian (no solve_scf). For frozen_core/manual/frontier (canonical MOs) mf.e_tot is exactly the active-space HF determinant energy, so validate_results Check 2 ('energy <= HF + 1e-5') is a valid variational sanity check (verified LiH: active FCI -7.8821 < mf.e_tot -7.8619). For mp2no/avas the ActiveSpace carries a ROTATED basis

### kanad reactions/ and dynamics/ (ReactionSimulator, QuantumReactionSimulator, MDSimulator, ConfigurationExplorer) vs the unified solver protocol (solvers/base_solver.py + core/solver_result.py)
_protocol: MIXED. The intended modern path — driving solvers through the builder's geometry-parametric `energy_fn()` closure `(atoms_bohr, warm_state) -> (energy_Ha, warm_state)` — is implemented correctly and conforms to the protocol in exactly two places: (1) QuantumReactionSimulator.from_system / callback mode, and (2) MDSimulator when a builder QuantumSystem is passed (delegating to dynamics/quantum_forces.compute_numerical_forces). Both correctly treat positions as Bohr, thread warm_state, and produce FCI-quality energies (verified: H2 at 0.74 Ang -> -1.13728, matching FCI; SQD lane == CI). The lega_

- [high/silent_wrong_result] ConfigurationExplorer silently computes HF regardless of solver_type ('sqd'/'vqe'/'hivqe' all ignored) — `/home/mk/deeprealm/kanad/analysis/configuration_explorer.py` — _get_molecular_energy (line ~475) calls molecule.compute_energy(solver=self.solver_type, backend=self.backend). But core/molecule.py:495 defines compute_energy(self, method='HF', **kwargs). 'solver=' and 'backend=' fall into **kwargs and are discarded; 'method' defaults to 'HF'. So with the DEFAULT solver_type='sqd' the explorer returns HF energies, not correlated SQD/FCI. Verified at runtime on H
- [high/silent_wrong_result] Angstrom/Bohr unit confusion makes legacy ReactionSimulator and QuantumReactionSimulator evaluate energies at the wrong geometry — `/home/mk/deeprealm/kanad/reactions/reaction_dynamics.py` — Atom.position is stored in Angstrom (verified: BondFactory H-H@0.74 -> position [0,0,0.74]). ReactionSimulator seeds self.atoms from reactant atoms (Angstrom values) but treats them as Bohr everywhere. _compute_quantum_energy (line 362) builds gto.M(atom=..., unit='bohr') directly from these Angstrom-valued numbers, so PySCF reads 0.74 as 0.74 Bohr = 0.39 Angstrom. Verified: ReactionSimulator quan
- [high/bug] IRC backward branch in QuantumReactionSimulator.compute_reaction_path climbs uphill (descend=False), producing non-physical negative barriers — `/home/mk/deeprealm/kanad/reactions/quantum_reaction.py` — Line 560: the reactant-side IRC step uses mass_weighted_irc_step(..., descend=False) (uphill). An IRC descends downhill on BOTH branches from the TS. This is the identical bug that reaction_dynamics.py:960-963 explicitly documents as already fixed ('descend=False made the reactant branch climb uphill, giving non-physical (negative) barriers'); the fix (descend=True on both branches) was never prop
- [medium/bug] QuantumReactionSimulator.compute_reaction_path never kicks off the TS along the reaction coordinate before steepest descent — `/home/mk/deeprealm/kanad/reactions/quantum_reaction.py` — Lines 538/555 start both IRC branches at pos = ts.geometry.copy() and immediately compute the gradient. At a converged TS |grad| ~ 0, so the guard `if np.linalg.norm(grad) > 1e-10` can break on the first iteration, leaving the remaining geometries[i]/energies[i] at their np.zeros initialization. barrier = ts.energy - energies[0] and reaction_energy = energies[-1] - energies[0] then use uninitializ
- [high/silent_wrong_result] Quantum energy path silently falls back to a crude hardcoded Morse/LJ potential on any exception — `/home/mk/deeprealm/kanad/reactions/reaction_dynamics.py` — ReactionSimulator.compute_energy (lines 336-338) wraps the quantum solve in try/except and on ANY exception returns _compute_classical_energy (a hardcoded Morse potential with D_e=0.17, r_e=1.4 for diatomics; generic -1/r + 0.1/r^6 otherwise). The Morse energies are on a completely different scale (verified -0.17 Ha at r=1.4 vs ~ -1.0 to -1.13 Ha for real H2). During a TS/IRC scan, a single PySCF/
- [medium/silent_wrong_result] ReactionSimulator._compute_quantum_energy hardcodes PhysicsVQE and ignores solver_method ('vqe'/'sqd'/'physics_vqe' all run PhysicsVQE) — `/home/mk/deeprealm/kanad/reactions/reaction_dynamics.py` — compute_energy dispatches the quantum path for solver_method in ['vqe','physics_vqe','sqd'] (line 329), but _compute_quantum_energy (lines 347-372) unconditionally constructs PhysicsVQE. Verified: the function source contains no branch on solver_method. So solver_method='sqd' or 'vqe' silently runs PhysicsVQE; the documented solver choice is a no-op. Misleading at best; combined with the unit bug,
- [medium/silent_wrong_result] compute_rate_constant has no guard against a non-physical negative activation energy -> overflow / inf rate — `/home/mk/deeprealm/kanad/reactions/reaction_dynamics.py` — compute_rate_constant (lines 1037-1062) computes E_act = ts.energy - E_reactant and then rate = (kT/h)*exp(-E_act_J/kT_J) with no check that E_act >= 0. Given the upstream bugs (unit confusion, negative IRC barriers, Morse fallback, or a TS search that converges to a minimum), E_act is easily negative, making exp(+large) -> astronomically large or inf rate and ~0 half-life, returned silently. The 
- [medium/bug] IRC initial direction falls back to a normalized near-zero gradient -> can divide by zero (NaN direction) — `/home/mk/deeprealm/kanad/reactions/reaction_dynamics.py` — TransitionState.reaction_coordinate is never assigned in find_transition_state (verified: no '.reaction_coordinate =' assignment exists), so compute_reaction_coordinate always takes the fallback at lines 939-941: direction = compute_gradient(ts.geometry); direction /= np.linalg.norm(direction). At a converged TS the gradient norm is ~0; if it is exactly 0 this is 0/0 = NaN (verified np.zeros/0.0 -
- [low/risk] Transition-state verification (imaginary frequency) is implemented only for diatomics; verified is always False for polyatomics — `/home/mk/deeprealm/kanad/reactions/reaction_dynamics.py` — _estimate_imaginary_frequency (lines 862-903) returns 0.0 for n_atoms != 2. find_transition_state sets verified = (|g| < conv) and (imag_freq is not None and imag_freq < 0). For any polyatomic TS imag_freq == 0.0, so verified can never be True -> the first-order-saddle check is non-functional beyond diatomics. This is honest (no false positives) but means polyatomic TS results carry no curvature v
- [low/protocol_violation] SolverResult API is used three inconsistent ways across these drivers; HybridSubspaceVQE still returns a plain dict — `/home/mk/deeprealm/kanad/reactions/quantum_reaction.py` — SolverResult is a frozen, NON-subscriptable dataclass exposing .energy and .to_dict() but NOT .n_evaluations (verified: r['energy'] raises TypeError; hasattr(r,'n_evaluations') is False). reaction_dynamics.py reads result.energy directly (OK); quantum_reaction.py uses solver.solve().to_dict()['energy'] for PhysicsVQE/VQESolver (OK) but for solver=='hybrid_subspace' (line 281-283) does result = sol
- [high/missing_test] Test coverage exercises only the callback path; every broken legacy path is untested, and the one ConfigurationExplorer test uses a mock with a non-matching signature that hides the real bug — `/home/mk/deeprealm/kanad/tests/validation/test_reactions_sqd.py` — tests/validation/test_reactions_sqd.py covers only QuantumReactionSimulator.from_system + scan_path + _irc; there are NO tests for legacy QuantumReactionSimulator (reactants=, solver='physics_vqe'), for ReactionSimulator at all, for find_transition_state/compute_reaction_path (IRC), or for compute_rate_constant. The IRC backward-descend bug and the unit bug therefore ship untested. Worse, tests/va

### kanad: bonds/ (core/bonds/bond_factory.py), backends/ (factory.py, base_backend.py, statevector_backend.py, planck_adapter.py, ibm/bluequbit/ionq), core/operators/ (fermion_operator.py, jordan_wigner.py) — audited against solvers/base_solver.py unified solver protocol
_protocol: PARTIAL. The local backends (statevector, planck) fully conform to the BaseBackend protocol and to BaseSolver's construction contract (verified: PlanckBackend.estimate_expectation matches StatevectorBackend to <1e-9; sample() bit-order matches the documented little-endian Qiskit convention; expectation_from_counts matches exact diagonal expectation). The operators are correct: jordan_wigner and build_molecular_hamiltonian_jw reproduce FCI to machine precision for H2 and (in the correct particle-number sector) HeH+; FermionOperator.normal_order/is_hermitian/copy/add are all correct with no alia_

- [high/protocol_violation] Cloud backends (ibm/bluequbit/ionq) violate the BaseBackend protocol: no subclassing, no `name` attr, no estimate_expectation/sample. BaseSolver.__init__ crashes at `self.backend.name`. — `/home/mk/deeprealm/kanad/backends/factory.py` — NEEDS CODE FIX. make_backend('ibm'/'bluequbit'/'ionq') returns IBMBackend/BlueQubitBackend/IonQBackend (backends/ibm/backend.py, backends/bluequbit/backend.py, backends/ionq/backend.py), none of which subclass BaseBackend (verified issubclass==False) or define a `name` attribute (verified AttributeError on obj.name). BaseSolver.__init__ line 61 does `self.backend_name = self.backend.name`, so any 
- [high/missing_test] Missing test: no protocol test asserts cloud backends conform to BaseBackend. The gap let the .name/estimate_expectation/sample violation ship. — `/home/mk/deeprealm/kanad/tests/unit/test_backend_protocol.py` — test_backend_protocol.py covers only statevector + planck-fallback. There is no test that make_backend('ibm'|'bluequbit'|'ionq') returns a BaseBackend with .name / estimate_expectation / sample. These can be checked without live credentials at the class level: assert issubclass(IBMBackend, BaseBackend) and hasattr(IBMBackend,'name') and callable methods exist. Such a parametrized conformance test 
- [high/silent_wrong_result] BondFactory.create_bond silently ignores `distance` when both atoms are Atom objects: positions are not updated, so the Hamiltonian uses the original geometry while get_bond_length() reports the requested distance. — `/home/mk/deeprealm/kanad/core/bonds/bond_factory.py` — NEEDS CODE FIX. create_bond only repositions atom_2 in the `isinstance(atom_2, str)` branch (lines 83-88). When BOTH inputs are Atom objects the `distance` kwarg is passed to the bond constructor (CovalentBond.__init__ stores it via BaseBond as metadata only) but the atoms keep their original positions, and CovalentHamiltonian is built from molecule positions (covalent_bond.py:82,95). Verified: cr
- [medium/silent_wrong_result] BondFactory.create_molecule silently ignores `basis` and `bond_types` parameters. — `/home/mk/deeprealm/kanad/core/bonds/bond_factory.py` — NEEDS CODE FIX (or doc/signature change). create_molecule advertises `basis: str = 'sto-3g'` and `bond_types` in its signature/docstring but never forwards either to Molecule (Molecule.__init__ does not even accept basis: signature is (atoms, bonds=None, spin=0, charge=0)). Verified: create_molecule(['H','H'], basis='6-31g') returns a Molecule with no basis attribute; the basis request is dropped,
- [low/risk] jordan_wigner with n_qubits=None on an identity-only/constant operator infers n_qubits=0 and emits a 0-qubit SparsePauliOp(['']) — a dimensionally mis-sized operator rather than an error. — `/home/mk/deeprealm/kanad/core/operators/jordan_wigner.py` — NEEDS TEST + small guard. _get_max_orbital returns -1 for an identity-only FermionOperator, so n_qubits = -1+1 = 0; jordan_wigner produces {'' : c} and jordan_wigner_sparse_pauli_op wraps it as SparsePauliOp([''], [c]) (a 0-qubit op). If a constant term were JW-transformed without an explicit n_qubits then combined with a multi-qubit Hamiltonian, the dimensions would not match (silent until a late
- [low/risk] PlanckBackend.estimate_expectation uses bare float(PauliSum.expectation(...)) without real_if_close, unlike StatevectorBackend; a complex-coefficient (non-Hermitian) observable would raise TypeError instead of returning the real part — divergent behavior between two backends presented as drop-in peers. — `/home/mk/deeprealm/kanad/backends/planck_adapter.py` — NEEDS CODE FIX for parity. Line 49 does float(PauliSum.from_qiskit(observable).expectation(sv)); the SparsePauliOp branch assumes a real scalar. StatevectorBackend.estimate_expectation (statevector_backend.py:26) defensively uses np.real_if_close. For Hermitian H (the VQE case) both agree to <1e-9 (verified), but for a non-Hermitian observable the planck path would raise TypeError('can't convert c
