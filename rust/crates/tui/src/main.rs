//! Placeholder TUI binary. Real ratatui implementation lands in Phase 3.4
//! once §6.3 (TUI log source) is decided.

fn main() -> anyhow::Result<()> {
    eprintln!("kanad-compute-tui: not implemented yet (Phase 3.4).");
    std::process::exit(2);
}
