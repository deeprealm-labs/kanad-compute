__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.eggs/
*.egg
.env
.venv
env/
venv/

# Bundled kanad framework is now COMMITTED so `pip install git+…` is self-contained
# (the framework's own repo is private). Re-vendor + commit it on framework updates.
